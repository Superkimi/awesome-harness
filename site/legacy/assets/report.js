const progress=document.querySelector('.reading-progress');
const updateProgress=()=>{const d=document.documentElement;const max=d.scrollHeight-d.clientHeight;progress.style.width=(max?d.scrollTop/max*100:0)+'%'};addEventListener('scroll',updateProgress,{passive:true});updateProgress();
document.querySelectorAll('.copy-code').forEach(btn=>btn.addEventListener('click',async()=>{const code=btn.closest('.code-card').querySelector('code').innerText;await navigator.clipboard.writeText(code);const old=btn.textContent;btn.textContent='已复制';setTimeout(()=>btn.textContent=old,1200)}));
const reportSearch=document.querySelector('[data-report-search]');if(reportSearch){reportSearch.addEventListener('input',()=>{const q=reportSearch.value.trim().toLowerCase();document.querySelectorAll('.finding').forEach(el=>el.classList.toggle('hidden',q&&!el.dataset.search.includes(q)))})}
const profileSearch=document.querySelector('[data-profile-search]');if(profileSearch){profileSearch.addEventListener('input',()=>{const q=profileSearch.value.trim().toLowerCase();document.querySelectorAll('.agent-profile').forEach(el=>el.classList.toggle('hidden',q&&!el.innerText.toLowerCase().includes(q)))})}
