1. 同事说选中的文件就是安全边界，我再往下查 shell 和权限，避免把“可见”误当成“隔离”。
2. 这一章不猜作者意图，只沿着固定版本的运行时代码、协议和测试看事实。
3. 固定版本证据：aider/coders/base_coder.py:2215-2240 · “聊天文件集”就是主要写权限边界；aider/coders/base_coder.py:2434-2485 · Shell 在宿主机执行，没有 OS 沙箱。
4. 事实一：文件有没有放进聊天，不只是上下文选择，也决定模型能不能直接改它。
5. 源码含义：Aider 把最重要的权限问题压缩成一个易懂交互，但它不是细粒度路径策略或 RBAC。
6. 事实二：命令执行前会问你，但点了同意以后就是在真实终端里跑；Aider 本身没有容器、seccomp 或工作区文件系统隔离。
7. 数据流：需求 → Repo Map/上下文 → Provider → Coder 编辑 → Git/日志交付。
8. 小白动作：先限制本次聊天文件集，再让模型 dry-run，确认 diff 后才授权写入。
9. 第二个动作：为每个自动动作保留 commit、日志和成本记录，失败时能回放。
10. 局限提醒：已审计文件加入、越界编辑和 shell 建议的人工确认。
11. 这一章的结论：Aider 把最重要的权限问题压缩成一个易懂交互，但它不是细粒度路径策略或 RBAC。
12. 下一章继续沿着固定提交的源码锚点，回答一个真实工作问题。

Fixed commit: 5dc9490bb35f9729ef2c95d00a19ccd30c26339c
