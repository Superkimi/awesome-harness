1. 任务跑到一半开始重复，我沿 watchdog 和 quality monitor 看它怎样自纠又及时停下。
2. 这一章不猜作者意图，只沿固定版本的运行时代码、扩展和测试看事实。
3. 固定版本证据：.pi/extensions/context-watchdog/index.ts:3-29 · 80% 中途压缩 watchdog 补上 pi 的长自主运行缺口；.pi/extensions/quality-monitor/index.ts:5-18 · 质量监控会 steer 自纠，但最多连续两次。
4. 事实一：模型若连续几十轮调用工具、不把控制权还给用户，原生 pi 可能迟迟不压缩；这个扩展在每一小轮都看油表，快满了就主动整理上下文再接着做。
5. 源码含义：长自治任务的上下文治理不能只挂在用户轮边界。
6. 事实二：模型答歪时 Harness 会马上插一句纠偏，但不会无限唠叨把自己困进循环。
7. 数据流：用户目标 → pi/扩展 → 上下文与工具约束 → 子 Agent 或文件动作 → session evidence。
8. 小白动作：先给任务设一个边界，再列输入、动作、检查和交付四格。
9. 第二个动作：遇到长任务先压缩输入，再给工具和子 Agent 设能力上限。
10. 局限提醒：已审计中途 watchdog、读结果裁剪、尾部注入、证据跨压缩保留。
11. 这一章的结论：长自治任务的上下文治理不能只挂在用户轮边界。
12. 下一章继续沿固定提交的源码锚点，回答一个真实工作问题。

Fixed commit: 0b7234031aabe56163e345792ce7a6ea05af321a
