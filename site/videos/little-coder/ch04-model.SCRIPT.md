1. 同事说提示词越多越聪明，我先看 KV cache、技能和知识注入如何控制顺序。
2. 这一章不猜作者意图，只沿固定版本的运行时代码、扩展和测试看事实。
3. 固定版本证据：.pi/extensions/_shared/inject.ts:1-27 · 动态知识放在对话尾部，保护 KV cache；.pi/extensions/skill-inject/index.ts:8-31 · 技能选择按失败恢复、近期工具、当前意图排序；.pi/extensions/knowledge-inject/index.ts:8-35 · 算法知识以关键词打分并反向声明所需工具。
4. 事实一：固定的历史前缀不动，只在最末尾塞这轮真正需要的小纸条，本地模型就不用每轮重算十几万 token 的缓存。
5. 源码含义：对本地推理，prompt 稳定性本身就是性能架构。
6. 事实二：它不会把整本工具手册都塞给小模型，只给最可能马上用到的几张卡；刚失败过的工具优先补课。
7. 数据流：用户目标 → pi/扩展 → 上下文与工具约束 → 子 Agent 或文件动作 → session evidence。
8. 小白动作：先给任务设一个边界，再列输入、动作、检查和交付四格。
9. 第二个动作：遇到长任务先压缩输入，再给工具和子 Agent 设能力上限。
10. 局限提醒：已审计固定 AGENTS、技能/知识选择、KV-cache 友好尾部注入与 dedupe。
11. 这一章的结论：对本地推理，prompt 稳定性本身就是性能架构。
12. 下一章继续沿固定提交的源码锚点，回答一个真实工作问题。

Fixed commit: 0b7234031aabe56163e345792ce7a6ea05af321a
