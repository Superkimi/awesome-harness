1. 老板要一张架构图，但上次 AI 画出来的箭头和含义全对不上。
2. Diagram Design 不是“多一个聊天框”，它把一个工作麻烦拆成了可观察的步骤。
3. 固定版本证据：skills/diagram-design/SKILL.md，画面只展示源码片段和中性文件名。
4. 事实一：先选语义模式再选图形类型，避免图形先行
5. 事实二：复杂度、连接器、文字间距和图例都有硬规则
6. 数据流：目标 → 状态/上下文 → 工具或节点 → 校验 → 结果交付。
7. 源码里能看到：curity matrix diagrams as standalone HTML/SVG/PNG. Redraw .drawio/.drawio.png/.drawio.svg or Mermaid .mmd。
8. 小白复现：先写目标，再列数据；每一步保留状态，最后再预览或交付。
9. 再看一个边界：它治理的是图的生成规范，不是执行 Agent
10. 所以它值得学的不是按钮，而是把过程变成可以检查的证据。
11. 今天记住：把它想成一本会被 Agent 读取的制图规范：先问图要表达什么，再决定用架构图、时序图、状态机还是数据流。
12. 下一条继续拆一个真实章节，先把问题说清，再让 Agent 负责重复劳动。

Fixed commit: 840f944f08be45eed52a6832d4930c11164a546f
