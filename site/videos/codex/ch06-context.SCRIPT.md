1. 长任务快超窗，我先看版本账本、工具配对、多模态成本和逐项删旧记录重试。
2. 这一章不猜作者意图，只沿固定版本的运行时代码、协议和测试看事实。
3. 固定版本证据：codex-rs/core/src/context_manager/history.rs:38-60 · 历史是带版本号的 Copy-on-Write 账本，不是随手拼接的消息数组；codex-rs/core/src/context_manager/history.rs:189-248 · 上下文裁剪同时维护工具调用配对并计算多模态成本；codex-rs/core/src/compact.rs:240-318 · 本地压缩会自救：压缩请求自身超限时逐项删旧记录再重试。
4. 事实一：像有版本号的账本：读取者共享同一份快照，真要改时才复制，且每笔工具调用都要能对上回执。
5. 源码含义：适合并发读取和回滚，也让 compaction、fork 与增量世界状态有明确一致性边界。
6. 事实二：剪历史不能只撕掉一页：工具问题和答案必须一起处理；图片、音频和加密内容也不能当作零体积。
7. 数据流：用户消息 → turn/step 快照 → Provider/工具 → 权限与沙箱 → rollout/SQLite 交付。
8. 小白动作：先把任务拆成状态快照、动作、审批和回放四格，再决定并发方式。
9. 第二个动作：把模型可见工具、真实执行器和审计事件分别记录，不要混成一张列表。
10. 局限提醒：审计 COW history、配对规范化、截断、本地/远端压缩和模型可见 token 估算。
11. 这一章的结论：适合并发读取和回滚，也让 compaction、fork 与增量世界状态有明确一致性边界。
12. 下一章继续沿固定提交的源码锚点，回答一个真实工作问题。

Fixed commit: 902bd9e06b3ecb32cbf7f8e64cd23b956be3e7fe
