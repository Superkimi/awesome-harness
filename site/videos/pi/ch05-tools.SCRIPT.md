1. 模型要改文件又读大输出，我先看并行策略、唯一替换和 read/bash 两套截断。
2. 这一章不猜作者意图，只沿固定版本的运行时代码、契约和测试看事实。
3. 固定版本证据：packages/agent/src/agent-loop.ts:411-553 · 工具默认可并行，声明 sequential 或全局策略才串行；packages/coding-agent/src/core/tools/edit.ts:33-53 · 编辑采用唯一精确替换，并对同一文件串行化；packages/coding-agent/src/core/tools/truncate.ts:1-12 · read 与 bash 使用不同方向的双阈值截断。
4. 事实一：互不冲突的工具一起跑更快；有副作用的工具可以声明排队。即使并发完成顺序不同，交给模型的账本仍按原顺序。
5. 源码含义：吞吐高，但自定义工具作者必须正确声明副作用和并发语义。
6. 事实二：它不凭模糊相似度猜改哪里；目标文字必须只出现一次。同一文件上的两把手术刀要排队，避免互相覆盖。
7. 数据流：用户消息 → turn/session → Provider 与工具 → compaction/权限 → JSONL 会话和交付。
8. 小白动作：先把长任务拆成主循环、上下文、工具、信任和观测五格。
9. 第二个动作：为并发、重试、编辑和回退各写一个明确终态，不要只看“运行中”。
10. 局限提醒：已审计 read/bash/edit/write、并行调度、截断、精确替换和同文件互斥。
11. 这一章的结论：吞吐高，但自定义工具作者必须正确声明副作用和并发语义。
12. 下一章继续沿固定提交的源码锚点，回答一个真实工作问题。

Fixed commit: 581d75a89cea21e50d6a26df840352f94427f633
