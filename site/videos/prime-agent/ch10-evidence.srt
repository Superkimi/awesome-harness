1
00:00:00,000 --> 00:00:10,800
评审问“恢复正确性怎么证明”，我用 parentId journal、四层 event 和测试布局回答。

2
00:00:11,000 --> 00:00:21,800
这一章不猜作者意图，只沿固定版本的运行时代码、契约和测试看事实。

3
00:00:22,000 --> 00:00:32,800
固定版本证据：packages/coding-agent/src/core/session-manager.ts:33-54 · SessionManager 用带 parentId 的 JSONL 树表达分支、压缩和扩展状态；packages/coding-agent/src/core/session-manager.ts:472-535 · Context 重建沿 parent tree，并把 compaction summary 放在 retained messages 前；packages/coding-agent/src/core/session-manager.ts:1345-1364 · 写盘采用临时文件 rename，普通 entry 采用 append-only。

4
00:00:33,000 --> 00:00:43,800
事实一：会话文件不是一条只能向后追加的聊天记录，而是一棵可导航的树；扩展可以持久化自己的 entry，又不会把内部状态强塞给模型。

5
00:00:44,000 --> 00:00:54,800
源码含义：要支持 fork、resume、compaction 和子 Agent，持久化格式需要 parent link、版本号和“不进入 LLM context”的 entry 类型。

6
00:00:55,000 --> 00:01:05,800
事实二：从任意分支恢复时，模型看到的是“摘要→保留的旧消息→新分支”，UI 还可以知道真实的树边界。

7
00:01:06,000 --> 00:01:16,800
数据流：纯 loop → coding host → resources/extensions → RLM/daemon → JSONL events 和恢复。

8
00:01:17,000 --> 00:01:27,800
小白动作：先把任务拆成 loop、工具、上下文、协作和持久化五格。

9
00:01:28,000 --> 00:01:38,800
第二个动作：为每个后台任务记录 admission、heartbeat、lease 和完成释放，避免幽灵进程。

10
00:01:39,000 --> 00:01:49,800
局限提醒：版本化 JSONL append-only session tree、原子 rewrite、typed events、telemetry/usage。

11
00:01:50,000 --> 00:02:00,800
这一章的结论：要支持 fork、resume、compaction 和子 Agent，持久化格式需要 parent link、版本号和“不进入 LLM context”的 entry 类型。

12
00:02:01,000 --> 00:02:11,800
下一章继续沿固定提交的源码锚点，回答一个真实工作问题。
